<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="29" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_acc_cleaner"
        android:label="ACC Cleaner"
        android:supportsRtl="true"
        android:theme="@style/Theme.AccCleaner">

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <activity android:name=".MainActivity" android:exported="false" />
        <activity android:name=".SafeMainActivity" android:exported="false" />
        <activity android:name=".ReviewMainActivity" android:exported="false" />
        <activity android:name=".ReviewMainActivityV104" android:exported="false" />
        <activity android:name=".WhatsAppBusinessCleanerActivity" android:exported="false" />

        <activity
            android:name=".CleanerHomeActivityV105"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
